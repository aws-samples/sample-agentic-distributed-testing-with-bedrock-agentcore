# Local Terraform working state — never commit
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfplan

# Real variable files may contain account-specific values — commit only the
# .example versions
terraform.tfvars
*.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
